좋아요 👍
아래는 **Go 인터페이스에서의 정적 디스패치(static dispatch)** 와 **동적 디스패치(dynamic dispatch)** 개념을 깔끔하게 정리한 버전입니다.
(Go의 디스패치는 대부분 인터페이스를 중심으로 구분됩니다.)

---

# 🧩 Go Interface의 정적·동적 디스패치 정리

---

## 1️⃣ 개념 비교 요약

| 구분           | 정적 디스패치 (Static Dispatch)    | 동적 디스패치 (Dynamic Dispatch)        |
| ------------ | ---------------------------- | --------------------------------- |
| **결정 시점**    | 컴파일 타임                       | 런타임                               |
| **호출 방식**    | 컴파일러가 호출 대상을 미리 확정           | 런타임에 실제 타입을 보고 호출 대상 결정           |
| **대표 상황**    | 구체 타입(Concrete Type)의 메서드 호출 | 인터페이스(Interface)를 통한 메서드 호출       |
| **오버헤드**     | 없음 (직접 호출)                   | 있음 (itab 탐색 + 간접 호출)              |
| **성능**       | 빠름 (인라인 가능)                  | 약간 느림 (런타임 lookup)                |
| **대표 구현 예시** | `r.Area()`                   | `shape.Area()` (shape은 interface) |

---

## 2️⃣ Go에서의 디스패치 흐름

### ✅ 정적 디스패치 (Static Dispatch)

* **함수나 구조체 메서드를 직접 호출할 때**
* 컴파일러가 호출 대상을 **이미 알고 있음**

```go
type Rect struct {
    W, H float64
}

func (r Rect) Area() float64 {
    return r.W * r.H
}

func main() {
    r := Rect{3, 4}
    fmt.Println(r.Area()) // ✅ 정적 디스패치
}
```

* `r.Area()` → 컴파일 시점에 `Rect.Area` 함수 주소로 직접 연결됨
* 인라인(inline) 최적화 가능
* 런타임 오버헤드 없음

---

### ⚙️ 동적 디스패치 (Dynamic Dispatch)

* **인터페이스 변수를 통해 메서드를 호출할 때 발생**
* 어떤 타입의 메서드를 호출할지는 **런타임에 결정**

```go
type Shape interface {
    Area() float64
}

type Circle struct {
    R float64
}

func (c Circle) Area() float64 {
    return 3.14 * c.R * c.R
}

func PrintArea(s Shape) {
    fmt.Println(s.Area()) // ⚙️ 동적 디스패치
}

func main() {
    c := Circle{R: 2}
    PrintArea(c)
}
```

* `s.Area()` 호출 시:

    1. 인터페이스 변수 `s` 내부의 `itab`(타입 + 메서드 테이블) 조회
    2. `Area()` 메서드 주소를 가져와 런타임에 호출
    3. 실제 구현체 `Circle.Area()` 실행

➡️ 즉, “어떤 `Area()`를 실행할지”는 **런타임에** 결정됨.

---

## 3️⃣ Go 인터페이스 내부 구조

Go 인터페이스 값은 내부적으로 **두 포인터**로 구성됩니다:

| 필드               | 설명                         |
| ---------------- | -------------------------- |
| **itab pointer** | 실제 타입 + 메서드 테이블(vtable 유사) |
| **data pointer** | 실제 값(예: `Circle{R:2}`)     |

```
+------------------+
|  itab pointer    | -> [타입 정보, 메서드 테이블]
+------------------+
|  data pointer    | -> [실제 데이터]
+------------------+
```

* 인터페이스 호출 시 `itab`을 참조해 메서드 주소를 찾고,
* `data` 포인터를 인자로 넘겨 호출합니다.

이 과정이 곧 **런타임 동적 디스패치**입니다.

---

## 4️⃣ 정적 디스패치 ↔ 동적 디스패치 비교 예시

```go
type Shape interface {
    Area() float64
}

type Rect struct{ W, H float64 }
func (r Rect) Area() float64 { return r.W * r.H }

func printRect(r Rect) { fmt.Println(r.Area()) }  // 정적 디스패치
func printShape(s Shape) { fmt.Println(s.Area()) } // 동적 디스패치

func main() {
    r := Rect{3, 4}
    printRect(r)   // ✅ 정적 디스패치
    printShape(r)  // ⚙️ 동적 디스패치
}
```

| 함수              | 호출 시점                     | 특징      |
| --------------- | ------------------------- | ------- |
| `printRect(r)`  | 컴파일 타임에 `Rect.Area`로 결정   | 정적 디스패치 |
| `printShape(r)` | 런타임에 `Rect`의 실제 구현을 찾아 호출 | 동적 디스패치 |

---

## 5️⃣ 제네릭(generics)과의 관계

Go 1.18 이후 등장한 **제네릭(generics)** 은
인터페이스를 사용하지 않고도 다형성을 구현하지만,
**모든 타입별로 코드가 컴파일 타임에 생성되므로 → 정적 디스패치**입니다.

```go
func PrintArea[T interface{ Area() float64 }](s T) {
    fmt.Println(s.Area()) // ✅ 정적 디스패치 (타입별로 컴파일)
}
```

→ 인터페이스를 통한 호출이 아니라, **컴파일 타임에 구체 타입별 함수가 만들어짐.**

---

## 6️⃣ 성능 차이 (요약)

| 항목        | 정적 디스패치       | 동적 디스패치               |
| --------- | ------------- | --------------------- |
| 결정 시점     | 컴파일 타임        | 런타임                   |
| 호출 비용     | 직접 호출         | 1회 간접 호출(itab lookup) |
| 인라인 가능성   | 매우 높음         | 거의 없음                 |
| 런타임 타입 체크 | 없음            | 있음                    |
| 대표 구현     | 구체 타입 호출, 제네릭 | 인터페이스 호출              |

---

## 7️⃣ 정리 요약표

| 구분                | 설명                      | 예시                                 |
| ----------------- | ----------------------- | ---------------------------------- |
| **정적 디스패치**       | 호출할 메서드가 **컴파일 시점에 결정** | `r.Area()`                         |
| **동적 디스패치**       | 호출할 메서드가 **런타임에 결정**    | `shape.Area()`                     |
| **동적 디스패치 발생 조건** | 인터페이스를 통해 메서드를 호출할 때    | `func Print(s Shape) { s.Area() }` |
| **Go에서 디스패치 종류**  | 두 가지 모두 지원              | 정적(기본), 동적(인터페이스)                  |
| **제네릭 디스패치**      | 타입별로 정적으로 생성되므로 정적 디스패치 | `func Print[T Shape](s T)`         |

---

## ✅ 결론 요약

> **Go는 기본적으로 정적 디스패치를 사용하지만**,
> **인터페이스를 통한 호출에서만 동적 디스패치가 발생한다.**
>
> 🔹 정적 디스패치 → 빠르고 컴파일 시 확정
> 🔹 동적 디스패치 → 인터페이스 기반 런타임 다형성 구현
> 🔹 제네릭 → 정적 디스패치 기반의 타입 안전한 다형성